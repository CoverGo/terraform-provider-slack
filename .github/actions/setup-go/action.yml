name: Setup go
description: Install Go and add workspace bin to PATH
runs:
  using: composite
  steps:
    - uses: actions/setup-go@v5
      with:
        go-version-file: go.mod
        cache: true
    - shell: bash
      run: echo '${{ github.workspace }}/bin' >> $GITHUB_PATH
